# Anthropic API Configuration
ANTHROPIC_API_KEY=your-actual-anthropic-api-key-here

# Git Configuration for RFE Workflows (Optional)
# These will be used by the claude-code-runner for RFE artifact management
GIT_USER_NAME="vTeam RFE Bot"
GIT_USER_EMAIL="vteam-rfe@example.com"

# Git Authentication (Optional)
# Direct secret values for Git operations
# GITHUB_TOKEN=ghp_your_github_personal_access_token_here
# GIT_TOKEN=your_git_token_for_non_github_providers
# GIT_SSH_KEY=your_ssh_private_key_content_here

# Legacy Git secret references (for backwards compatibility)
# GIT_SSH_KEY_SECRET=git-ssh-key
# GIT_TOKEN_SECRET=git-token

# Git Repositories (Optional)
# JSON array of repositories to clone on startup
# GIT_REPOSITORIES='[{"url": "https://github.com/org/repo.git", "branch": "main"}]'

# Git Workspace Settings
GIT_CLONE_ON_STARTUP=false
# Runner Secret configuration (must match ProjectSettings.spec.runnerSecretsName and namespace)
# Defaults used by deploy.sh: ambient-runner-secrets in ambient-code namespace
RUNNER_SECRET_NAME=ambient-runner-secrets
RUNNER_SECRET_NAMESPACE=ambient-code

# Container Registry Configuration
CONTAINER_REGISTRY=quay.io/ambient_code
IMAGE_TAG=latest

# OpenShift oauth-proxy sidecar configuration (simple)
# Required: create OAuthClient and set the client secret below
# OCP_OAUTH_CLIENT_SECRET=replace-me
# Optional: override cookie secret (random if empty)
# OCP_OAUTH_COOKIE_SECRET=replace-me
# Optional: set a specific Route host
# ROUTE_HOST=ambient-code.apps.example.com
